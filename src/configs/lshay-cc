import { Zone, Record } from "@pulumi/cloudflare";

const zone = new Zone(
	"lshay-cc",
	{
		plan: "free",
		zone: "lshay.cc",
	},
	{
		protect: true,
	},
);

new Record("ui-cname", {
  name: "ui",
  proxied: false,
  type: "CNAME",
  value: "lukeshay.github.io",
  zoneId: zone.id,
});
